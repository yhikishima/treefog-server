<!DOCTYPE html>
<%#include "user.h" %>
<% tfetch(QVariantMap, user); %>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= controller()->name() + ": " + controller()->activeAction() %></title>
</head>
<body>
<p style="color: red"><%=$ error %></p>
<p style="color: green"><%=$ notice %></p>

<h1>Editing User</h1>

<%== formTag(urla("save", user["id"]), Tf::Post) %>
  <p>
    <label>ID<br /><input type="text" name="user[id]" value="<%= user["id"] %>" readonly="readonly" /></label>
  </p>
  <p>
    <label>Name<br /><input type="text" name="user[name]" value="<%= user["name"] %>" /></label>
  </p>
  <p>
    <label>Password<br /><input type="text" name="user[password]" value="<%= user["password"] %>" /></label>
  </p>
  <p>
    <input type="submit" value="Save" />
  </p>
</form>

<%== linkTo("Show", urla("show", user["id"])) %> |
<%== linkTo("Back", urla("index")) %>
</body>
</html>
